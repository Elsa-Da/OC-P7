import './carousel.scss';

function Carousel() {
  return <p>lol</p>;
  /** return (
    <div>
      {pic.map((i, index) => (
        <div>
          <img src={i} />
        </div>
      ))}
    </div>
  );
}
*/
}
export default Carousel;
